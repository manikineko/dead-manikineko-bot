import { CommandInteraction } from 'discord.js';
import type { CommandConfig } from 'robo.js';
export declare const config: CommandConfig;
declare const _default: (interaction: CommandInteraction) => Promise<"You need to provide a message to send!" | "Invalid channel." | "The specified channel is not a text channel." | {
    content: string;
    ephemeral: boolean;
}>;
export default _default;
