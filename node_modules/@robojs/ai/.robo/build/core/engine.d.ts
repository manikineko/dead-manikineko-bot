interface Mode {
    id: string;
    name: string;
    systemMessage?: string;
}
declare function getAllModes(): Promise<Mode[]>;
declare function upsertMode(mode: Omit<Mode, 'id'>): Promise<Mode>;
export declare const AiEngine: {
    Modes: {
        getAll: typeof getAllModes;
        upsert: typeof upsertMode;
    };
};
export {};
