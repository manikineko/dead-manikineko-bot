import { BaseEngine } from '@/engines/base.js';
import type { ChatMessage, ChatOptions, ChatResult, GenerateImageOptions, GenerateImageResult } from '@/engines/base.js';
import type { Command } from 'robo.js';
/**
 * AI engine powered by OpenAI.
 * You must have an OpenAI API key in order to use this engine.
 */
export declare class OpenAiEngine extends BaseEngine {
    private _assistant?;
    private _gptFunctions;
    private _gptFunctionHandlers;
    /**
     * Prepares the Assistant instance and optimizes commands for GPT Functions.
     */
    init(): Promise<void>;
    chat(messages: ChatMessage[], options: ChatOptions): Promise<ChatResult>;
    generateImage(options: GenerateImageOptions): Promise<GenerateImageResult>;
    getFunctionHandlers(): Record<string, Command>;
    getInfo(): {
        name: string;
        version: any;
    };
}
