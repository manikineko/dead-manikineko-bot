import { Client } from 'discord.js';
import type { BaseEngine } from '@/engines/base.js';
export interface PluginOptions {
    commands?: boolean | string[];
    engine?: BaseEngine;
    insight?: boolean;
    maxTokens?: number;
    model?: string;
    pollDelay?: number;
    restrict?: {
        channelIds: string[];
    };
    systemMessage?: string;
    temperature?: number;
    whitelist?: {
        channelIds: string[];
    };
}
export declare let options: PluginOptions;
/**
 * Converts all portal commands into GPT functions for later use
 */
declare const _default: (_client: Client, pluginOptions: PluginOptions) => void;
export default _default;
