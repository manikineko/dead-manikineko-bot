import type { Message, Typing } from 'discord.js';
/**
 * When a user currently being replied to starts typing, delay the reply.
 * This allows us to have a more natural conversation flow and better context.
 */
declare const _default: (event: Typing) => void;
export default _default;
export declare function addUserFollowUp(userId: string, message: Message, messageContent: string): void;
export declare function clearUserTyping(userId: string): void;
export declare function isUserTyping(userId: string): boolean;
export declare function waitForTyping(userId: string): Promise<string[]>;
