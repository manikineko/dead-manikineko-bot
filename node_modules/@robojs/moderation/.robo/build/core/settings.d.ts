export interface GuildSettings {
    auditLogsChannelId?: string;
    logsChannelId?: string;
    mailChannelId?: string;
    lockdownMode?: boolean;
    requireConfirmation?: boolean;
    testMode?: boolean;
}
export declare function getSettings(guildId: string | null): GuildSettings;
export declare function updateSettings(guildId: string | null, settings: GuildSettings): GuildSettings;
