//put certian commands behind a paywall except if the user is power level 10(Beta Tester) or above
//read commands/setlevel.ts for more information on power levels

import { Flashcore, type CommandConfig } from 'robo.js'


//this is a middleware function
export default function (data: { payload: [any] }) {
	
}